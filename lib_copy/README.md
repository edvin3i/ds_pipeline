# Управление библиотеками плагинов DeepStream

Эта папка содержит скрипты для управления установкой пользовательских плагинов DeepStream.

## Скрипты

### 1. `collect_libs.sh` - Сбор библиотек

Собирает скомпилированные библиотеки плагинов из исходных папок проекта в `lib_copy/`.

**Использование:**
```bash
./collect_libs.sh
```

**Что делает:**
- Копирует `libnvtilebatcher.so` из `my_tile_batcher/`
- Копирует `libnvdsstitch.so` из `my_steach/`
- Копирует `libnvdsvirtualcam.so` из `my_virt_cam/`
- Показывает статистику скопированных файлов

**Пример вывода:**
```
✓ Скопировано: libnvtilebatcher.so (56K)
✓ Скопировано: libnvdsstitch.so (96K)
✓ Скопировано: libnvdsvirtualcam.so (86K)

✓ Скопировано: 3 библиотек
```

---

### 2. `install_to_system.sh` - Установка в систему

Устанавливает собранные библиотеки из `lib_copy/` в системную папку GStreamer.

**Использование:**
```bash
./install_to_system.sh
```

**Что делает:**
- Копирует все `.so` файлы из `lib_copy/` в `~/.local/share/gstreamer-1.0/plugins/`
- GStreamer автоматически находит плагины в этой папке
- Показывает список установленных плагинов

**Пример вывода:**
```
⚠ Заменяем: libnvtilebatcher.so
  ✓ Установлено (56K)

✓ Установлено: 3 библиотек

📦 Установленные плагины в системе:
  libnvdsstitch.so
  libnvdsvirtualcam.so
  libnvtilebatcher.so
```

---

## Типичный workflow

### При компиляции изменений в плагине:

```bash
# 1. Перейти в папку плагина и скомпилировать
cd /home/nvidia/deep_cv_football/my_tile_batcher/src
make

# 2. Собрать библиотеки в lib_copy
cd /home/nvidia/deep_cv_football/lib_copy
./collect_libs.sh

# 3. Установить в систему
./install_to_system.sh

# 4. Проверить установку
gst-inspect-1.0 nvtilebatcher
```

### Быстрая установка всех плагинов:

```bash
cd /home/nvidia/deep_cv_football/lib_copy
./collect_libs.sh && ./install_to_system.sh
```

---

## Проверка установленных плагинов

После установки можно проверить что плагины видны GStreamer:

```bash
# Проверка nvtilebatcher
gst-inspect-1.0 nvtilebatcher

# Проверка nvdsstitch
gst-inspect-1.0 nvdsstitch

# Проверка nvdsvirtualcam
gst-inspect-1.0 nvdsvirtualcam

# Список всех установленных плагинов
ls -lh ~/.local/share/gstreamer-1.0/plugins/
```

---

## Альтернатива: `make install` в каждом плагине

Вместо использования этих скриптов, можно установить плагины напрямую из их исходных папок:

```bash
# nvtilebatcher
cd /home/nvidia/deep_cv_football/my_tile_batcher/src
make install

# nvdsstitch
cd /home/nvidia/deep_cv_football/my_steach
make install

# nvdsvirtualcam
cd /home/nvidia/deep_cv_football/my_virt_cam
make install
```

**Преимущества скриптов lib_copy:**
- ✅ Централизованное управление
- ✅ Быстрая переустановка всех плагинов одной командой
- ✅ Резервная копия скомпилированных библиотек
- ✅ Удобно при частых изменениях

---

## Структура папок

```
lib_copy/
├── collect_libs.sh           # Скрипт сбора библиотек
├── install_to_system.sh      # Скрипт установки в систему
├── README.md                 # Эта документация
├── libnvtilebatcher.so       # Библиотеки (после collect_libs.sh)
├── libnvdsstitch.so
└── libnvdsvirtualcam.so
```

---

## Удаление плагинов из системы

Если нужно удалить плагины:

```bash
rm ~/.local/share/gstreamer-1.0/plugins/libnvtilebatcher.so
rm ~/.local/share/gstreamer-1.0/plugins/libnvdsstitch.so
rm ~/.local/share/gstreamer-1.0/plugins/libnvdsvirtualcam.so
```

---

**Создано:** 2025-10-18
**Версия:** 1.0
