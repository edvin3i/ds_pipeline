# nvtilebatcher - GStreamer плагин для детекции на панорамах

**GStreamer плагин для вырезания 6 тайлов из панорамы футбольного поля и передачи их батчем в nvinfer для детекции мяча.**

---

## 📖 Описание

**nvtilebatcher** - это кастомный GStreamer элемент для NVIDIA DeepStream, который:

1. **Принимает** панораму 6528×1632 (от nvdsstitch)
2. **Вырезает** 6 тайлов размером 1024×1024 с заданных координат
3. **Создает** батч из 6 тайлов с правильными метаданными DeepStream
4. **Передает** батч в nvinfer для детекции мяча

### Зачем это нужно?

- ✅ Детекция мелких объектов (мяч) на большой панораме
- ✅ Эффективная батч-обработка через nvinfer (batch-size=6)
- ✅ Сохранение координат тайлов для обратного маппинга детекций
- ✅ Zero-copy операции на Jetson через EGL/CUDA

---

## 🎯 Позиция в пайплайне

```
nvstreammux (2 камеры)
    ↓
nvdsstitch (создает панораму 6528×1632)
    ↓
nvtilebatcher (вырезает 6 тайлов 1024×1024) ← ВЫ ЗДЕСЬ
    ↓
nvinfer (детекция мяча на батче из 6 тайлов)
    ↓
... обработка результатов ...
```

---

## 🚀 Быстрый старт

### 1. Компиляция

```bash
make
```

Проверка:
```bash
gst-inspect-1.0 nvtilebatcher
```

### 2. Запуск всех тестов

```bash
./run_all_tests.sh
```

### 3. Использование в пайплайне

```python
pipeline_str = """
    ... ваш source ...
    ! nvstreammux name=m batch-size=2 width=3840 height=2160
    ! nvdsstitch left-source-id=0 right-source-id=1
    ! nvtilebatcher
    ! nvinfer config-file-path=yolo_config.txt batch-size=6
    ! ... downstream ...
"""
```

---

## 📁 Файлы проекта

### Основной код

| Файл | Описание |
|------|----------|
| `gstnvtilebatcher.cpp` | Главный файл плагина (860 строк) |
| `gstnvtilebatcher.h` | Заголовочный файл с структурами |
| `gstnvtilebatcher_allocator.cpp` | Аллокатор памяти для батч-буферов |
| `gstnvtilebatcher_allocator.h` | Заголовок аллокатора |
| `cuda_tile_extractor.cu` | CUDA kernel для вырезания тайлов |
| `Makefile` | Сборка проекта |

### Тесты

| Файл | Что тестирует |
|------|---------------|
| `test_batch_structure.py` | Структура батча (6 тайлов, метаданные) |
| `test_complete_pipeline.py` | Полный пайплайн с детекцией |
| `test_full_pipeline.py` | Сохранение тайлов для визуального осмотра |
| `run_all_tests.sh` | Автоматический запуск всех тестов |

### Документация

| Файл | Содержание |
|------|------------|
| `README.md` | Этот файл - обзор проекта |
| `QUICKSTART.md` | Быстрый старт для нетерпеливых |
| `TESTING_GUIDE.md` | Подробное руководство по тестированию |
| `CHECKLIST.md` | Чек-лист для диагностики |

---

## 🧪 Тестирование

### Быстрая проверка (10 секунд)

```bash
python3 test_batch_structure.py
```

Проверяет что батч создается правильно.

### Полная проверка (30 секунд)

```bash
python3 test_complete_pipeline.py
```

Проверяет весь пайплайн с детекцией мяча.

### Все тесты автоматически

```bash
./run_all_tests.sh
```

Запускает все проверки и показывает итоговый отчет.

📖 **Детали:** см. [TESTING_GUIDE.md](TESTING_GUIDE.md)

---

## ⚙️ Конфигурация

### Параметры плагина

```bash
nvtilebatcher gpu-id=0 silent=false
```

| Параметр | Тип | Значение по умолчанию | Описание |
|----------|-----|----------------------|----------|
| `gpu-id` | uint | 0 | ID GPU для обработки |
| `silent` | boolean | false | Отключить info сообщения |

### Позиции тайлов

Жестко заданы в коде ([gstnvtilebatcher.h:34-41](gstnvtilebatcher.h#L34)):

```cpp
{0, 0,    316},  // Тайл 0: левый край
{1, 1088, 316},  // Тайл 1
{2, 2176, 316},  // Тайл 2: центр
{3, 3264, 316},  // Тайл 3
{4, 4352, 316},  // Тайл 4
{5, 5440, 316},  // Тайл 5: правый край
```

Чтобы изменить - редактируйте `DEFAULT_TILE_POSITIONS` и перекомпилируйте.

---

## 🔧 Архитектура

### Основные компоненты

1. **Фиксированный пул буферов**
   - 4 буфера предварительно созданы при старте
   - Ротация по кругу для минимизации аллокаций
   - Каждый буфер содержит NvBufSurface с 6 surfaces (тайлами)

2. **EGL/CUDA интеграция**
   - Zero-copy доступ к памяти на Jetson
   - Входные буферы кешируются с помощью хеш-таблицы
   - Выходные буферы регистрируются один раз при старте

3. **CUDA kernel**
   - Один запуск для всех 6 тайлов (Z-dimension = 6)
   - Использует `__constant__` память для позиций
   - Векторизованное копирование (uchar4)

4. **Метаданные DeepStream**
   - Копирует метаданные из входного буфера
   - Создает 6 `NvDsFrameMeta` (по одному на тайл)
   - Добавляет `TileRegionInfo` как user_meta с координатами

### Поток данных

```
1. submit_input_buffer()
   ↓
2. Получение входной панорамы (NvBufSurface)
   ↓
3. Регистрация EGL ресурсов (если нужно)
   ↓
4. Получение выходного буфера из фиксированного пула
   ↓
5. Установка указателей на тайлы в CUDA
   ↓
6. Запуск CUDA kernel (extract_tiles_kernel_multi)
   ↓
7. Синхронизация CUDA
   ↓
8. Создание/обновление метаданных DeepStream
   ↓
9. Отправка буфера downstream
   ↓
10. generate_output() (возвращает NULL)
```

---

## 🐛 Решение проблем

### Проблема: Плагин не найден

```bash
gst-inspect-1.0 nvtilebatcher
# Выдает: No such element or plugin
```

**Решение:**
```bash
# Проверить компиляцию
make clean && make

# Проверить что .so файл создан
ls -la libnvtilebatcher.so

# Добавить в путь GStreamer (если нужно)
export GST_PLUGIN_PATH=$(pwd):$GST_PLUGIN_PATH
```

### Проблема: Неправильное количество тайлов

```
num_frames_in_batch: 1 (expected 6)
```

**Решение:**
```bash
# Проверить что batchSize устанавливается правильно
# В gstnvtilebatcher.cpp:500-501 должно быть:
# output_surface->batchSize = 6;
# output_surface->numFilled = 6;

# Перекомпилировать
make clean && make
```

### Проблема: Черные тайлы

**Решение:**
1. Проверить размер входной панорамы (должен быть 6528×1632)
2. Проверить координаты в `DEFAULT_TILE_POSITIONS`
3. Посмотреть логи CUDA kernel

Полный список проблем: [TESTING_GUIDE.md - Решение проблем](TESTING_GUIDE.md#решение-проблем)

---

## 📊 Производительность

### Ожидаемые показатели

| Платформа | FPS (6 тайлов) | GPU Memory | CPU Usage |
|-----------|----------------|------------|-----------|
| Jetson Xavier NX | 25-30 | ~500 MB | 15-20% |
| Jetson Orin | 50-60 | ~500 MB | 10-15% |
| x86 + RTX 3090 | 100+ | ~300 MB | <10% |

### Оптимизации

- ✅ Один CUDA kernel для всех 6 тайлов
- ✅ Фиксированный пул буферов (нет runtime аллокаций)
- ✅ Constant memory для позиций тайлов
- ✅ Векторизованное копирование (uchar4)
- ✅ EGL кеширование для входных буферов
- ✅ Предварительная регистрация выходных буферов

---

## 🔗 Связь с другими компонентами

### Upstream: nvdsstitch

nvdsstitch должен создавать панораму с параметрами:
- Размер: 6528×1632 pixels
- Формат: RGBA
- Memory: NVMM (SURFACE_ARRAY на Jetson)
- Метаданные: NvDsBatchMeta с source_id

### Downstream: nvinfer

nvinfer должен быть настроен на:
- `batch-size=6`
- `network-mode=0` (FP32) или `=1` (FP16) или `=2` (INT8)
- Размер входа модели: 1024×1024 (или с `maintain-aspect-ratio=1`)
- Custom parser для YOLO (если используется YOLO)

---

## 📚 Документация

- **Быстрый старт:** [QUICKSTART.md](QUICKSTART.md)
- **Подробное тестирование:** [TESTING_GUIDE.md](TESTING_GUIDE.md)
- **Чек-лист диагностики:** [CHECKLIST.md](CHECKLIST.md)
- **DeepStream SDK:** https://docs.nvidia.com/metropolis/deepstream/dev-guide/
- **GStreamer Plugin Guide:** https://gstreamer.freedesktop.org/documentation/plugin-development/

---

## 🤝 Вклад

Если нашли баг или хотите добавить фичу:

1. Проверьте что проблема воспроизводится с тестами
2. Добавьте тест для нового функционала
3. Убедитесь что все тесты проходят: `./run_all_tests.sh`
4. Обновите документацию если нужно

---

## 📄 Лицензия

Proprietary - для использования в проекте deep_cv_football

---

## 📧 Контакты

- **Проект:** deep_cv_football
- **Компонент:** tilebatcher
- **Версия:** 1.0
- **Дата:** 2025-10-09

---

**Создано с помощью Claude Code** 🤖
