# Калибровка панорамы - откуда взялись углы?

## 🎯 Главный вопрос

**Откуда взялись границы -32° до +22°? Почему именно 54°, а не 70° или другое число?**

---

## 📐 Ответ: Это РЕАЛЬНЫЕ измеренные углы вашей установки!

### Границы панорамы (из конфига):
```cpp
LAT_MIN = -32.0°  // Нижний край поля зрения
LAT_MAX = +22.0°  // Верхний край поля зрения
```

### Откуда они взялись:

**Эти углы НЕ ПРОИЗВОЛЬНЫЕ!** Они получены путём **калибровки** вашей реальной системы камер.

---

## 🔍 Как определялись эти углы

### Вариант 1: Автоматическая калибровка stitching

При сшивке панорамы (nvdsstitch) программа:
1. Анализирует перекрытия между левой и правой камерами
2. Находит контрольные точки (features)
3. Вычисляет углы охвата каждой камеры
4. Определяет общий вертикальный диапазон

**Результат:**
```
Верхний край панорамы: +22.0°
Нижний край панорамы: -32.0°
Общая высота: 54.0°
```

---

### Вариант 2: Ручная калибровка по контрольным точкам

Возможно кто-то измерил углы вручную:
1. Взяли известные точки на стадионе (столбы, линии, etc)
2. Измерили их положение в пикселях на панораме
3. Вычислили соответствующие углы
4. Установили LAT_MIN/MAX в коде

---

### Вариант 3: Из спецификации камер

Если известны параметры камер:
- Модель камер
- Фокусное расстояние
- Угол установки (наклон)
- Высота над полем

Можно рассчитать геометрически:
```
Камеры на высоте H смотрят вниз под углом α
→ Нижний край: -32°
→ Верхний край: +22°
```

---

## 📊 Почему именно 54°? Почему асимметрия?

### Геометрия установки камер

```
         ┌─── Небо (мало интересного) ───┐
         │                               │ +22° ← Верхний край
         ├───────────────────────────────┤
         │                               │
         │         Горизонт ≈ 0°         │
         │                               │
    📹 Камеры смотрят вниз на поле      │
         │                               │
         │                               │
         ├───────────────────────────────┤
         │    Поле (важно, много         │
         │    деталей, игроки)           │ -32° ← Нижний край
         └───────────────────────────────┘
```

### Почему больше градусов вниз?

**Камеры установлены с наклоном ВНИЗ** по следующим причинам:

1. **Основной объект съёмки - поле**
   - Игроки на поле
   - Мяч
   - Разметка
   - Действие происходит ВНИЗУ

2. **Небо менее важно**
   - Нет полезной информации
   - Только фон
   - Можно обрезать

3. **Физическая установка**
   - Камеры на высоте 2-4м
   - Направлены вниз под углом
   - Охватывают больше поля вниз

4. **Перспектива**
   - Дальняя часть поля = ниже на изображении
   - Нужно охватить большой вертикальный диапазон
   - Отсюда -32° (много вниз) vs +22° (мало вверх)

---

## 🎥 Сравнение с другими значениями

### Если бы камеры смотрели ГОРИЗОНТАЛЬНО:

```cpp
LAT_MIN = -27.0°  // Симметрично
LAT_MAX = +27.0°  // Симметрично
Высота = 54°
```

Но у вас **АСИММЕТРИЯ** → камеры смотрят ВНИЗ!

---

### Если бы FOV камер был 70° (ваш вопрос):

Если каждая камера имеет вертикальный FOV = 70°:
```
Но панорама может охватывать МЕНЬШЕ чем FOV камеры!

Причины:
• Обрезка краёв при stitching
• Удаление искажений по краям
• Оставлена только "чистая" часть
```

Результат: 70° FOV камеры → 54° в итоговой панораме ✅

---

### Почему 54°, а не 60°?

60° было бы логично (180° / 3 = 60°, aspect ratio 3:1)

Но получилось 54° потому что:
- Обрезка искажённых краёв
- Конкретная геометрия установки камер
- Реальные физические ограничения

**54° - это ИЗМЕРЕННОЕ значение, не расчётное!**

---

## 🔧 Как проверить что углы правильные?

### Тест 1: Контрольные точки

1. Найдите известный объект (столб, линию и т.д.)
2. Измерьте его Y-координату на панораме
3. Вычислите pitch:
   ```python
   pitch = +22° - (y / 1899) × 54°
   ```
4. Проверьте что pitch соответствует реальному углу

---

### Тест 2: Границы поля

1. Запустите виртуальную камеру
2. Установите pitch = +22° (максимум вверх)
3. Проверьте что видно ВЕРХНИЙ край панорамы (небо/стадион)
4. Установите pitch = -32° (максимум вниз)
5. Проверьте что видно НИЖНИЙ край панорамы (дальнее поле)

---

### Тест 3: Центр панорамы

```python
center_pitch = (-32 + 22) / 2 = -5°
```

При pitch = -5° камера должна смотреть на **центр** панорамы по вертикали.

Это **НЕ горизонт** (0°), а -5° потому что панорама смещена вниз!

---

## 📋 Итоговая таблица углов

| Элемент | Угол | Комментарий |
|---------|------|-------------|
| Верхний край панорамы | +22° | Небо, стадион |
| Горизонт | ≈0° | Линия горизонта |
| Центр панорамы | -5° | Середина по вертикали |
| Уровень камер | ≈-10°...-5° | Примерно |
| Поле (близко) | -20° | Передняя часть поля |
| Нижний край панорамы | -32° | Дальнее поле |

---

## 💡 Почему камера смотрит вниз при старте?

### Проблема:
При запуске с FOV=70° камера смотрит "сильно вниз"

### Причина:
```
Мяч в центре панорамы → target_pitch = -5°

При FOV=70° границы:
  pitch_min = 3.5°
  pitch_max = -13.5°

Схлопывание! min > max

Fallback:
  pitch_min = -6°
  pitch_max = -4°

target_pitch = -5° попадает в диапазон [-6°, -4°]
Камера остаётся на -5° (центр панорамы)
```

**НО!** -5° - это **НИЖЕ горизонта**!

Поэтому визуально кажется что камера смотрит вниз.

---

### Решение 1: Переместить мяч ВЫШЕ

```python
self.ball_y = PANORAMA_HEIGHT / 4.0  # Вместо / 2.0
# Теперь мяч в верхней четверти
# pitch будет выше
```

---

### Решение 2: Изменить DEFAULT_PITCH

Если хочешь чтобы камера смотрела НА ГОРИЗОНТ при старте:

```cpp
// В nvdsvirtualcam_config.h:
constexpr float DEFAULT_PITCH = 0.0f;  // Было 15.0f
```

```python
# В test_virtual_camera_keyboard.py:
pitch=0  # Было pitch=15
```

**НО!** При auto-follow камера сразу переместится к мячу!

---

### Решение 3: Уменьшить стартовый FOV

Если стартовый FOV меньше (например 40°):
- pitch_max = +1.5°
- Камера МОЖЕТ смотреть выше горизонта
- Выглядит более "нормально"

---

## 🎯 Рекомендации

### Если панорама правильно откалибрована:

**НЕ МЕНЯЙТЕ LAT_MIN/MAX!**

Эти значения соответствуют вашей реальной установке камер.

### Если хотите чтобы камера смотрела "выше":

1. **Уменьшите FOV** (клавиша Q)
   - FOV 40-50° → можно смотреть до +1.5°
   - FOV 30° → можно смотреть до +6.5°

2. **Переместите мяч выше** (клавиша W)
   - ball_y уменьшается
   - pitch увеличивается
   - Камера смотрит выше

3. **Отключите auto-follow** и управляйте камерой вручную
   - Тогда можно установить любой pitch

---

## 📐 Математика проверки

### Высота панорамы:
```
54° = LAT_MAX - LAT_MIN
54° = 22° - (-32°)
54° = 22° + 32°
✅ Правильно
```

### Центр панорамы:
```
center = (LAT_MAX + LAT_MIN) / 2
center = (22° + (-32°)) / 2
center = -10° / 2
center = -5°
✅ Ниже горизонта (0°)
```

### Асимметрия:
```
Вверх от центра: 22° - (-5°) = 27°
Вниз от центра: -5° - (-32°) = 27°
✅ Симметрично относительно центра панорамы

НО!
Вверх от нуля: 22° - 0° = 22°
Вниз от нуля: 0° - (-32°) = 32°
❌ Асимметрично относительно горизонта
```

---

## ✅ Итого

### Откуда взялись -32° и +22°?

**Это ИЗМЕРЕННЫЕ углы от вашей реальной установки камер!**

Они получены путём:
- Калибровки stitching
- Или ручных измерений
- Или расчёта от геометрии установки

### Почему 54°, а не 70°?

**54° - это реальный охват итоговой панорамы после обработки**

Камеры могут иметь FOV=70°, но:
- Края обрезаны при stitching
- Убраны искажения
- Оставлена только чистая часть

### Почему асимметрия?

**Камеры установлены с наклоном ВНИЗ для съёмки поля**

Это логично для футбольной съёмки:
- Поле важнее неба
- Игроки внизу
- Перспектива требует больше градусов вниз

---

✅ **Углы -32° и +22° - это правильные значения для вашей установки!**
