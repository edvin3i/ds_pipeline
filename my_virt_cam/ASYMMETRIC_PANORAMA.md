# Проблема асимметричной панорамы

## 🔍 Обнаруженная проблема

**Даже при малом FOV камера НЕ МОЖЕТ подняться высоко!**

### Почему так происходит?

## 📐 Геометрия панорамы

### Текущие границы (из конфига):
```cpp
LAT_MIN = -32°  // Низ поля
LAT_MAX = +22°  // Верх поля
```

### Асимметрия:
```
Центр панорамы = (-32 + 22) / 2 = -5°

Расстояния от центра:
  Вниз:  -5° до -32° = 27° ⬇️
  Вверх: -5° до +22° = 27° ⬆️

❌ СТОП! Это НЕ ПРАВИЛЬНО!
   27° вниз ≠ 32° вниз (от центра до LAT_MIN)
```

Правильный расчёт:
```
От центра (-5°) до:
  Низа (-32°):  |-32 - (-5)| = 27° вниз
  Верха (+22°): |+22 - (-5)| = 27° вверх

НО! От нуля:
  До низа:  -32° (32° вниз от 0)
  До верха: +22° (22° вверх от 0)

Панорама СМЕЩЕНА ВНИЗ!
```

---

## 📊 Влияние на ограничения

### Формула ограничения:
```cpp
pitch_max = LAT_MAX - half_fov_v - MARGIN
pitch_max = +22° - half_fov_v - 0.5°
```

### Таблица ограничений:

| FOV | half_fov | pitch_max | Может подняться? |
|-----|----------|-----------|------------------|
| 30° | 15°      | **+6.5°** | ✅ Да, выше 0° |
| 35° | 17.5°    | **+4.0°** | ✅ Да, выше 0° |
| 40° | 20°      | **+1.5°** | ✅ Да, чуть выше 0° |
| 43° | 21.5°    | **0.0°**  | ⚠️ На нуле |
| 44.2° | 22.1°  | **-0.6°** | ❌ Ниже нуля |
| 50° | 25°      | **-3.5°** | ❌ Ниже середины |
| 60° | 30°      | **-8.5°** | ❌ Далеко внизу |
| 70° | 35°      | **-13.5°**| ❌ Очень низко |

**Критическая точка: FOV ≈ 43°**
- При FOV > 43° камера НЕ МОЖЕТ подняться выше горизонта (0°)!
- При FOV > 48° камера НЕ МОЖЕТ подняться выше середины панорамы (-5°)!

---

## 🎯 Почему панорама асимметрична?

### Возможные причины:

#### 1. Камеры установлены низко
```
Если камеры на уровне поля:
  ↑ Небо занимает мало места (+22°)
  ↓ Поле видно далеко вниз (-32°)
```

#### 2. Перспектива футбольного поля
```
При съёмке с уровня поля:
  ↑ Верхняя граница = горизонт + немного неба
  ↓ Нижняя граница = поле уходит вдаль

Логично что поле внизу занимает больше градусов!
```

#### 3. Stitching обрезал верх
```
Возможно при сшивке панорамы:
  ↑ Верхняя часть обрезана (нет интересного контента)
  ↓ Нижняя часть сохранена (важно для поля)
```

---

## 🔧 Решения

### Решение 1: Увеличить LAT_MAX (если есть запас в панораме)

Если в панораме **есть контент выше +22°**, можно увеличить:

```cpp
// В nvdsvirtualcam_config.h:
constexpr float LAT_MAX = +35.0f;  // Было +22.0f
```

**Результат:**
- Высота панорамы: 67° (вместо 54°)
- При FOV=44° pitch_max = +35 - 22.1 - 0.5 = **+12.4°** ✅

**Как проверить что можно увеличить LAT_MAX:**
1. Откройте исходное видео панорамы
2. Посмотрите на самый верх кадра (y=0)
3. Есть ли там полезный контент (небо, стадион)?
4. Если да - можно увеличить LAT_MAX!

---

### Решение 2: Уменьшить LAT_MIN (если есть запас внизу)

Если внизу панорамы **нет важного контента**, можно уменьшить:

```cpp
// В nvdsvirtualcam_config.h:
constexpr float LAT_MIN = -25.0f;  // Было -32.0f
```

**Результат:**
- Высота панорамы: 47° (меньше чем было 54°)
- При FOV=44° pitch_max = +22 - 22.1 - 0.5 = **-0.6°** (без изменений)

❌ Это НЕ поможет для подъёма камеры вверх!

---

### Решение 3: Использовать малый FOV для подъёма

**Самое простое решение:**

```python
# Для подъёма камеры вверх - уменьшите радиус мяча:
ball_radius = 5-15px  → FOV = 30-40°  → pitch_max = +1.5° до +6.5°
```

**Управление:**
- Нажмите **Q** несколько раз → radius уменьшится → FOV уменьшится
- Теперь камера может подняться выше!
- Нажмите **W** → камера поднимется вверх

---

## 📊 Практическая таблица

### Чтобы поднять камеру на определённую высоту:

| Нужен pitch_max | Макс FOV | Макс radius | Как достичь |
|----------------|----------|-------------|-------------|
| +6.5° (высоко) | 30°      | 5px         | Q до минимума |
| +4.0° (выше 0) | 35°      | 10.5px      | Q несколько раз |
| +1.5° (чуть выше 0) | 40° | 15.5px      | Q пару раз |
| 0.0° (горизонт) | 43°     | 18.7px      | Стартовый - чуть Q |
| -0.6° (чуть ниже 0) | 44.2° | 20px       | Стартовый |
| -3.5° (ниже середины) | 50° | 26px       | E несколько раз |
| -13.5° (далеко вниз) | 70° | 60px       | E до максимума |

---

## 🎮 Как пользоваться

### Для широкого обзора (стартовый):
```
ball_radius = 60px → FOV = 70°
+ Широкий обзор
- Камера смотрит вниз (pitch=-13.5° максимум вверх)
- Вертикальное движение почти заблокировано
```

### Для подъёма камеры вверх:
```
1. Нажмите Q несколько раз
2. Радиус уменьшится до 5-15px
3. FOV станет 30-40°
4. Нажмите W - камера поднимется до +1.5° ... +6.5°
```

### Для слежения за мячом внизу:
```
ball_radius = 20-40px → FOV = 44-63°
+ Хороший баланс
+ Камера может смотреть вниз
- Не может смотреть высоко вверх
```

---

## 🔍 Отладка границ в логах

### Включить логи:
```bash
GST_DEBUG=nvdsvirtualcam:3 python3 test_virtual_camera_keyboard.py ...
```

### Что смотреть:
```
INFO: FOV=44.2° → Границы: yaw[-44.0, 44.0], pitch[-9.4, -0.6]
                                                         ↑
                                                   pitch_max = -0.6°
                                                   НЕ МОЖЕТ выше!
```

Если видите `pitch[-X, -0.6]` - камера НЕ МОЖЕТ подняться выше -0.6°

---

## 💡 Рекомендации

### Если панорама действительно ограничена +22° сверху:

**Живите с этим ограничением:**
- Используйте малый FOV (30-40°) для съёмки неба/высоких объектов
- Используйте большой FOV (60-70°) для обзора поля

### Если панорама шире чем +22°:

**Исправьте LAT_MAX в конфиге:**
1. Проверьте исходное видео - до какого угла есть контент
2. Обновите `LAT_MAX` в `nvdsvirtualcam_config.h`
3. Пересоберите плагин
4. Проверьте границы в логах

---

## 📋 Итого

### Проблема:
```
Панорама асимметрична:
  Низ:  -32° (много места вниз)
  Верх: +22° (мало места вверх)

Из-за этого даже при малом FOV камера не может сильно подняться.
```

### Решение:
```
1. Проверьте реальные границы панорамы
2. Если есть контент выше +22° → увеличьте LAT_MAX
3. Иначе используйте малый FOV (Q) для подъёма камеры
```

✅ **Это нормальное ограничение если панорама снята с уровня поля!**
