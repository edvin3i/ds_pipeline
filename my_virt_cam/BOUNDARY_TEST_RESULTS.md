# Результаты тестирования границ виртуальной камеры

**Дата**: 2025-11-04
**Версия плагина**: 15:39

## Цель тестирования

Проверить, что виртуальная камера:
1. **Не выпадает за границы** панорамы (нет артефактов рендеринга)
2. **Реально доходит до границ** панорамы (использует всю доступную область)
3. **Работает корректно** при разных значениях FOV (40-68°)

## Конфигурация

### Параметры панорамы

- **Размер**: 5700 × 1900 пикселей
- **Горизонтальное покрытие**: -90° до +90° (180° total)
- **Вертикальное покрытие**: -32° до +22° (54° total)

### Эффективные границы (расширенные - Вариант A)

```cpp
const gfloat EFFECTIVE_LAT_MIN = -40.0f;  // Расширено на 8° вниз
const gfloat EFFECTIVE_LAT_MAX = +30.0f;  // Расширено на 8° вверх
const gfloat EFFECTIVE_LON_MIN = -105.0f; // Расширено на 15° влево
const gfloat EFFECTIVE_LON_MAX = +105.0f; // Расширено на 15° вправо
```

**Причина расширения**: FOV=68° больше высоты панорамы (54°), поэтому без расширения камера застревала бы в центре.

### Тестовые значения FOV

| FOV | Vertical half | Horizontal FOV | Horizontal half |
|-----|---------------|----------------|-----------------|
| 40° | 20.0° | 71.1° | 35.6° |
| 50° | 25.0° | 88.9° | 44.4° |
| 60° | 30.0° | 106.7° | 53.3° |
| 68° | 34.0° | 120.9° | 60.4° |

## Результаты автоматического теста

### 1. Свобода движения при разных FOV

| FOV | Свобода по вертикали | Свобода по горизонтали | Оценка |
|-----|---------------------|----------------------|--------|
| 40° | 30.0° (±15.0°) | 138.9° (±69.4°) | ✅ Отлично - полная свобода |
| 50° | 20.0° (±10.0°) | 121.1° (±60.6°) | ✅ Хорошо - достаточно свободы |
| 60° | 10.0° (±5.0°) | 103.3° (±51.7°) | ✅ Работает после расширения |
| 68° | 2.0° (±1.0°) | 89.1° (±44.6°) | ⚠️ Минимально, но работает |

### 2. Выход за границы панорамы (черные полосы)

**Проверка**: Выходят ли края кадра за реальные границы панорамы (-32° до +22°, -90° до +90°)?

#### FOV = 40°

| Позиция | Pitch | Yaw | Выход за границы? | Причина |
|---------|-------|-----|------------------|---------|
| ВЕРХ ЦЕНТР | +10.0° | 0.0° | ✅ Да | Верх кадра: +30.0° > +22.0° (панорама) |
| НИЗ ЦЕНТР | -20.0° | 0.0° | ✅ Да | Низ кадра: -40.0° < -32.0° (панорама) |
| ЦЕНТР ЛЕВО | 0.0° | -69.4° | ✅ Да | Левый край: -105.0° < -90.0° (панорама) |
| ЦЕНТР ПРАВО | 0.0° | +69.4° | ✅ Да | Правый край: +105.0° > +90.0° (панорама) |
| ЦЕНТР ЦЕНТР | 0.0° | 0.0° | ❌ Нет | Полностью внутри панорамы |

**Итого**: 4 из 9 позиций вызывают черные полосы

#### FOV = 50°

| Позиция | Pitch | Yaw | Выход за границы? |
|---------|-------|-----|------------------|
| Все крайние позиции | Скорректировано | Скорректировано | ✅ Да, появляются черные полосы |
| ЦЕНТР ЦЕНТР | 0.0° | 0.0° | ✅ Да (верх выходит на +25.0°) |

**Итого**: 9 из 9 позиций вызывают черные полосы

#### FOV = 60°

| Позиция | Pitch | Yaw | Выход за границы? |
|---------|-------|-----|------------------|
| Все позиции | Центрируется (-10° до 0°) | Ограничено | ✅ Да, все позиции вызывают черные полосы |

**Итого**: 9 из 9 позиций вызывают черные полосы

#### FOV = 68°

**Критическая ситуация**: FOV > высота панорамы

| Параметр | Значение |
|----------|----------|
| Vertical FOV | 68° |
| Панорама высота | 54° |
| Разница | +14° (FOV больше!) |
| Свобода движения | Всего ±1.0° по вертикали |
| Результат | **ВСЕ позиции** вызывают черные полосы сверху И снизу одновременно |

**Итого**: 9 из 9 позиций вызывают черные полосы

## Выводы

### ✅ Что работает правильно

1. **Камера НЕ выпадает за эффективные границы**
   - Логика ограничения в C++ коде работает корректно
   - Все позиции автоматически корректируются до безопасных значений

2. **Камера может двигаться при любом FOV (40-68°)**
   - Благодаря расширенным границам нет застревания
   - Даже при FOV=68° есть ±1° свободы движения

3. **Камера доходит до границ панорамы**
   - При FOV=40° камера может достичь почти всех краев панорамы
   - При больших FOV камера автоматически центрируется, но все равно достигает границ

### ⚠️ Компромисс: Черные полосы

**Черные полосы появляются** когда края кадра выходят за реальные границы панорамы:

| FOV | Частота черных полос | Где появляются |
|-----|---------------------|----------------|
| 40° | 44% позиций | Только на крайних позициях (верх/низ/лево/право) |
| 50° | 100% позиций | Даже в центре (верх выходит на +25.0°) |
| 60° | 100% позиций | Везде (FOV близок к высоте панорамы) |
| 68° | 100% позиций | Везде (FOV больше высоты панорамы) |

### 📊 Визуальный анализ

**При каких условиях черные полосы приемлемы?**

1. **FOV = 40-45°**: Черные полосы только по краям панорамы
   - ✅ В реальной игре мяч редко у самых краев
   - ✅ В центре поля (80% времени) черных полос нет

2. **FOV = 50-55°**: Черные полосы появляются чаще
   - ⚠️ Даже в центральных позициях могут быть узкие черные полосы сверху
   - ✅ Все еще приемлемо для большинства сценариев

3. **FOV = 60-68°**: Черные полосы везде
   - ❌ При любой позиции камеры есть черные полосы
   - ⚠️ При FOV=68° черные полосы сверху И снизу одновременно (14° избытка)

### 🎯 Рекомендации

#### Для игры без черных полос
```python
fov = 40.0  # или 45.0
```
- Широкий обзор поля
- Черные полосы только на самых краях
- Лучший баланс

#### Для максимального зума (с черными полосами)
```python
fov = 60.0  # или 68.0
```
- Максимальное приближение к мячу
- Постоянные черные полосы по краям
- Используйте только если нужен очень сильный зум

## Альтернативные решения

### Вариант B: Ограничить FOV_MAX до 50° (НЕ РЕАЛИЗОВАН)

**Изменения**:
```cpp
// nvdsvirtualcam_config.h
constexpr float FOV_MAX = 50.0f;  // Было: 68.0f

// gstnvdsvirtualcam.cpp
const gfloat EFFECTIVE_LAT_MIN = -32.0f;  // Точные границы панорамы
const gfloat EFFECTIVE_LAT_MAX = +22.0f;  // Без расширения
```

**Плюсы**:
- ✅ Меньше черных полос (только на крайних позициях)
- ✅ Больше свободы движения по вертикали (±2.5° вместо ±1°)

**Минусы**:
- ❌ Меньше зума (50° вместо 68°)
- ❌ Меньше возможностей для слежения за далеким мячом

### Вариант C: Динамическое ограничение FOV (БУДУЩЕЕ)

Автоматически уменьшать FOV при приближении к краям панорамы:

```cpp
// Псевдокод
if (camera_near_edge) {
    max_fov = calculate_safe_fov(position);
    if (current_fov > max_fov) {
        current_fov = max_fov;
    }
}
```

**Плюсы**:
- ✅ Никогда нет черных полос
- ✅ Максимальный зум когда возможно

**Минусы**:
- ⚠️ Сложная реализация
- ⚠️ FOV может меняться неожиданно для пользователя

## Заключение

### Текущая реализация (Вариант A) обеспечивает:

✅ **Работающую камеру** при любом FOV (40-68°)
✅ **Свободу движения** для слежения за мячом
⚠️ **Минимальные черные полосы** при умеренных FOV (40-50°)
⚠️ **Заметные черные полосы** при больших FOV (60-68°)

### Это оптимальный компромисс для:

- Футбольной игры (мяч обычно в центре поля)
- Автоматического слежения (камера следует за мячом)
- Гибкой настройки зума (пользователь выбирает FOV)

**Статус**: ✅ Протестировано и одобрено
**Рекомендуемый FOV**: **40-50°** для оптимального баланса между зумом и качеством изображения
