#!/bin/bash
# Быстрая пересборка плагина виртуальной камеры

echo "🔄 Пересборка плагина..."
cd "$(dirname "$0")/src"

# Полная пересборка
make clean && make && make install

if [ $? -eq 0 ]; then
    echo ""
    echo "✅ Плагин успешно пересобран!"
    echo "📁 Расположение: $(pwd)/libnvdsvirtualcam.so"
    ls -lh libnvdsvirtualcam.so
else
    echo ""
    echo "❌ Ошибка при пересборке!"
    exit 1
fi
