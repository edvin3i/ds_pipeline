# Тестирование границ панорамы

## 🎯 Проблема

При старте камеры видны **чёрные области внизу** → границы неправильные!

---

## 🧪 План тестирования

Сейчас мяч на **середине панорамы** (y=950px):
```python
ball_y = PANORAMA_HEIGHT / 2.0  # 950px
ball_radius = 60.0  # FOV=70°
```

Камера должна смотреть на **середину панорамы** → pitch ≈ -5°

---

## 📐 Варианты границ для тестирования

### Текущие границы (68°):
```cpp
LAT_MIN = -39.0°  // Низ
LAT_MAX = +29.0°  // Верх
Центр = -5°
```

**Если видны чёрные области внизу → LAT_MIN слишком низко (панорама меньше)!**

---

### Вариант 1: Попробуем 70° высоту (симметрично от -5°)
```cpp
LAT_MIN = -40.0°  // -5° - 35° = -40°
LAT_MAX = +30.0°  // -5° + 35° = +30°
Высота = 70°
```

**Когда использовать:** Если FOV=70° помещается ИДЕАЛЬНО без чёрного

---

### Вариант 2: Попробуем 64° высоту
```cpp
LAT_MIN = -37.0°  // -5° - 32° = -37°
LAT_MAX = +27.0°  // -5° + 32° = +27°
Высота = 64°
```

**Когда использовать:** Если видны небольшие чёрные полосы при FOV=68-70°

---

### Вариант 3: Попробуем 60° высоту
```cpp
LAT_MIN = -35.0°  // -5° - 30° = -35°
LAT_MAX = +25.0°  // -5° + 30° = +25°
Высота = 60°
```

**Когда использовать:** Если видны значительные чёрные области при FOV=70°

---

### Вариант 4: Попробуем 56° высоту
```cpp
LAT_MIN = -33.0°  // -5° - 28° = -33°
LAT_MAX = +23.0°  // -5° + 28° = +23°
Высота = 56°
```

**Когда использовать:** Если чёрные области большие при FOV=70°

---

## 🔍 Как определить правильные границы

### Метод 1: Визуальная проверка

**Шаг 1:** Запустите с текущими границами
```bash
python3 test_virtual_camera_keyboard.py ../new_week/left.mp4 ../new_week/right.mp4
```

**Шаг 2:** Посмотрите на экран:
- **Чёрные полосы ВНИЗУ?** → LAT_MIN слишком низко, нужно увеличить (например -39° → -35°)
- **Чёрные полосы ВВЕРХУ?** → LAT_MAX слишком высоко, нужно уменьшить (например +29° → +25°)
- **Чёрное И ВНИЗУ И ВВЕРХУ?** → Панорама меньше, уменьшите обе границы симметрично

**Шаг 3:** Измерьте размер чёрной полосы в пикселях:
```
Высота кадра: 1080px
Чёрная полоса внизу: X px

Процент чёрного = X / 1080 * 100%

Нужно уменьшить высоту панорамы на:
  Δ = 68° * (процент_чёрного / 100)
```

---

### Метод 2: Уменьшить FOV и проверить края

**Шаг 1:** Нажмите **Q много раз** → FOV уменьшится до 40°

**Шаг 2:** Нажмите **W много раз** → камера поднимется ВВЕРХ до границы

**Что должно быть видно:**
- Если видно **КРАЙ ПАНОРАМЫ** (верхняя часть изображения, небо) → LAT_MAX правильно ✅
- Если кадр **ПОЛНОСТЬЮ ЗАПОЛНЕН**, можно ещё выше → LAT_MAX можно увеличить

**Шаг 3:** Нажмите **S много раз** → камера опустится ВНИЗ до границы

**Что должно быть видно:**
- Если видно **КРАЙ ПАНОРАМЫ** (нижняя часть изображения, дальнее поле) → LAT_MIN правильно ✅
- Если видны **ЧЁРНЫЕ ОБЛАСТИ** → LAT_MIN слишком низко, нужно увеличить!

---

## 🛠️ Как быстро изменить границы

### Способ 1: Редактировать и пересобрать

```bash
# 1. Откройте конфиг
nano src/nvdsvirtualcam_config.h

# 2. Измените строки 21-22:
#    LAT_MIN = -XX.0f;
#    LAT_MAX = +YY.0f;

# 3. Пересоберите
cd src && make clean && make

# 4. Запустите тест снова
cd .. && python3 test_virtual_camera_keyboard.py ...
```

---

### Способ 2: Автоматический скрипт

```bash
# Создайте скрипт для быстрого изменения границ
./quick_test_boundaries.sh -37.0 27.0
# Это установит LAT_MIN=-37°, LAT_MAX=+27° и пересоберёт
```

---

## 📊 Сводная таблица

| Высота панорамы | LAT_MIN | LAT_MAX | Когда использовать |
|----------------|---------|---------|-------------------|
| 70° | -40° | +30° | FOV=70° идеально без чёрного |
| 68° | -39° | +29° | FOV=70° почти без чёрного (2% чёрное) |
| 64° | -37° | +27° | FOV=70° небольшие полосы (~5% чёрное) |
| 60° | -35° | +25° | FOV=70° видны полосы (~12% чёрное) |
| 56° | -33° | +23° | FOV=70° большие полосы (~17% чёрное) |

**Центр всегда сохраняется на -5°!**

---

## ✅ Контрольный список

- [ ] Запустил тест с текущими границами (68°)
- [ ] Проверил наличие чёрных областей
- [ ] Измерил размер чёрной области в процентах
- [ ] Выбрал новые границы из таблицы
- [ ] Обновил `src/nvdsvirtualcam_config.h`
- [ ] Пересобрал плагин (`make clean && make`)
- [ ] Запустил тест снова
- [ ] Повторил пока не найдены идеальные границы

---

## 💡 Рекомендация

**Если видны чёрные области внизу при FOV=70°:**

Попробуйте **Вариант 3** (60° высота):
```cpp
LAT_MIN = -35.0f;
LAT_MAX = +25.0f;
```

Это уменьшит высоту панорамы на 8° и должно убрать чёрные области!

---

**Сообщите какой процент чёрного видно и я порекомендую точные границы!**
