# Краткая сводка исправлений виртуальной камеры

## Основная проблема
При максимальном зуме (FOV=68°) и позиции камеры у края панорамы появлялись чёрные полосы сверху/снизу/по бокам.

## Решение
Добавлено **ограничение (clamping)** для FOV с учётом сферической геометрии.

---

## Ключевые исправления

### 1. Инвертированная формула pitch ❌→✅
**Где:** `gstnvdsvirtualcam.cpp:117-139`

**Было:**
```cpp
*out_pitch = LAT_MIN - norm_y * (LAT_MIN - LAT_MAX);  // НЕПРАВИЛЬНО
```

**Стало:**
```cpp
*out_pitch = LAT_MAX - norm_y * (LAT_MAX - LAT_MIN);  // ПРАВИЛЬНО
```

---

### 2. Центрирование границ панорамы 🎯
**Где:** `nvdsvirtualcam_config.h:21-22`

**Было:** -32° до +22° (54° высота, несимметрично)
**Стало:** -27° до +27° (54° высота, симметрично)

---

### 3. Коррекция для сферической геометрии 🌐
**Где:** `gstnvdsvirtualcam.cpp:1366-1393`

Когда камера повёрнута (yaw ≠ 0), вертикальное движение создаёт более длинную дугу.

**Решение:**
```cpp
gfloat yaw_factor = cosf(yaw_normalized * M_PI * 0.5f);
gfloat corrected_half_fov_v = effective_half_fov_v / fmaxf(yaw_factor, 0.6f);
```

**⚠️ ВАЖНО:** Используется **ДЕЛЕНИЕ**, не умножение!

---

### 4. Ограничение (clamping) FOV 🔒
**Где:** `gstnvdsvirtualcam.cpp:1395-1425`

**Проблема:**
```
corrected_half_fov_v > 27° → pitch_min >= pitch_max → НЕВАЛИДНЫЕ ГРАНИЦЫ → чёрные полосы
```

**Решение:**
```cpp
// Вертикаль: запас -0.2° (подобрано итеративно)
corrected_half_fov_v = fminf(corrected_half_fov_v, max_half_panorama_v - 0.2f);

// Горизонталь: запас -0.1° (подобрано итеративно)
clamped_half_fov_h = fminf(effective_half_fov_h, max_half_panorama_h - 0.1f);
```

---

## Итерации подбора запасов

### Вертикальный запас:
1. -1.0° → "вроде лучше стало, давай увеличим"
2. -0.5° → "да, давай ещё немного увеличим"
3. **-0.2°** → ✅ **Финальное значение**

### Горизонтальный запас:
1. -0.5° → "не заметил разницы, давай немного увеличим"
2. -0.2° → "давай немного увеличим, полоса всё ещё видна"
3. **-0.1°** → ✅ **Финальное значение**

---

## Параметры системы

| Параметр | Значение | Комментарий |
|----------|----------|-------------|
| Размер панорамы | 5700×1900 px | |
| Вертикальные границы | -27° до +27° | 54° покрытие |
| Горизонтальные границы | -90° до +90° | 180° покрытие |
| FOV диапазон | 40° до 68° | |
| Spherical Factor V | 0.635 | 54/68 × 0.8 |
| Spherical Factor H | 0.63 | Эмпирически |
| Вертикальный запас | -0.2° | Итеративно |
| Горизонтальный запас | -0.1° | Итеративно |
| Smooth factor | 0.3 | По умолчанию |

---

## Как это работает

### 1. Без ограничения (старая версия):
```
FOV=68° at yaw=-80° (у края):
corrected_half_fov_v = 43° / 0.7 ≈ 61° 🔴 БОЛЬШЕ ПАНОРАМЫ!

pitch_min = -27 + 61 = +34° 🔴
pitch_max = +27 - 61 = -34° 🔴
→ pitch_min > pitch_max (невалидно!)
→ fallback → чёрные полосы ❌
```

### 2. С ограничением (новая версия):
```
FOV=68° at yaw=-80°:
corrected_half_fov_v = 43° / 0.7 ≈ 61°
clamped = min(61°, 27° - 0.2°) = 26.8° ✅ ОГРАНИЧЕНО!

pitch_min = -27 + 26.8 = -0.2° ✅
pitch_max = +27 - 26.8 = +0.2° ✅
→ валидный диапазон [−0.2°, +0.2°]
→ нет чёрных полос ✅
```

---

## Быстрая навигация по коду

### Основные секции (с подробными комментариями):

1. **Преобразование координат** → строка 117
2. **Сглаживание камеры** → строка 150
3. **Слежение за мячом** → строка 1256
4. **Расчёт границ (ГЛАВНОЕ!)** → строка 1310
   - Сферические коэффициенты → строка 1340
   - Yaw factor коррекция → строка 1366
   - Clamping (ограничение) → строка 1395
   - Двухуровневая защита → строка 1464

---

## Полная документация

Для детального описания каждого исправления см. **[FIXES_DOCUMENTATION.md](FIXES_DOCUMENTATION.md)**

---

## Сборка

```bash
cd /home/nvidia/deep_cv_football/my_virt_cam
./rebuild.sh
```

---

## Тестирование

```bash
cd /home/nvidia/deep_cv_football/my_virt_cam
python3 test_virtual_camera_keyboard.py ../new_week/left.mp4 ../new_week/right.mp4
```

**Управление:**
- Стрелки: движение камеры
- `+`/`-`: зум
- Q: выход

**Проверьте:**
1. Камера в центре → двигается свободно ✅
2. Максимальный зум (FOV=68°) + верхний край → нет чёрных полос ✅
3. Максимальный зум + нижний край → нет чёрных полос ✅
4. Максимальный зум + левый/правый край → нет чёрных полос ✅
5. Поворот камеры + движение вверх/вниз → не выходит за границы ✅

---

## Известные ограничения

При **очень крайних позициях** (углы панорамы) и **максимальном зуме** (FOV=68°) могут появляться **минимальные чёрные полосы**.

Это нормально и ожидаемо:
- FOV=68° больше высоты панорамы (54°)
- Запасы -0.2° и -0.1° дают максимум свободы
- Альтернатива (больший запас) → камера застревает в центре

**Компромисс:** Максимум свободы движения vs абсолютное отсутствие полос

Мы выбрали **максимум свободы** ✅
