# Объяснение ограничений движения камеры

## 📐 Как работают ограничения

### Основной принцип

**Границы движения камеры = динамически рассчитываются на основе текущего FOV**

Чем **больше FOV** → тем **меньше свобода движения**

---

## 🔢 Математика ограничений

### Входные данные:

```cpp
// Границы панорамы (из конфига)
LAT_MIN = -32°  // Низ поля
LAT_MAX = +22°  // Верх поля
MARGIN = 0.5°   // Технический запас

// Текущий FOV
FOV = 40-70°    // Вертикальный угол обзора камеры
```

### Формула расчёта границ:

```cpp
// 1. Половина FOV
half_fov_v = FOV / 2

// 2. Границы для pitch (вертикальное движение)
pitch_min = LAT_MIN + half_fov_v + MARGIN  // Нижний предел
pitch_max = LAT_MAX - half_fov_v - MARGIN  // Верхний предел

// 3. Применение ограничений
pitch = CLAMP(pitch, pitch_min, pitch_max)
```

---

## 📊 Примеры расчётов

### Пример 1: FOV = 40° (узкий - свобода движения)

```
Шаг 1: Половина FOV
half_fov_v = 40° / 2 = 20°

Шаг 2: Рассчитываем границы
pitch_min = -32° + 20° + 0.5° = -11.5°
pitch_max = +22° - 20° - 0.5° = +1.5°

Результат:
┌────────────────────────────────────────────────┐
│ Диапазон движения: от -11.5° до +1.5° = 13°   │
│ Свобода движения: ✅ ПОЛНАЯ                   │
└────────────────────────────────────────────────┘

Что это означает:
• Камера МОЖЕТ смотреть ВВЕРХ до +1.5°
• Камера МОЖЕТ смотреть ВНИЗ до -11.5°
• 13° свободы для вертикального движения
```

**Почему такие границы?**

```
Визуализация кадра при pitch = +1.5° (максимум вверх):

Камера видит от:
  Верх кадра: +1.5° + 20° = +21.5°  ✅ (в пределах панорамы +22°)
  Низ кадра:  +1.5° - 20° = -18.5°  ✅ (в пределах панорамы -32°)

Всё влезает! Можно смотреть вверх до +1.5°


Камера видит от:
  Верх кадра: -11.5° + 20° = +8.5°   ✅ (в пределах панорамы +22°)
  Низ кадра:  -11.5° - 20° = -31.5°  ✅ (в пределах панорамы -32°)

Всё влезает! Можно смотреть вниз до -11.5°
```

---

### Пример 2: FOV = 50° (средний - ограниченное)

```
Шаг 1: Половина FOV
half_fov_v = 50° / 2 = 25°

Шаг 2: Рассчитываем границы
pitch_min = -32° + 25° + 0.5° = -6.5°
pitch_max = +22° - 25° - 0.5° = -3.5°

Результат:
┌────────────────────────────────────────────────┐
│ Диапазон движения: от -6.5° до -3.5° = 3°     │
│ Свобода движения: ⚠️ ОГРАНИЧЕНА               │
└────────────────────────────────────────────────┘

Что это означает:
• Камера МОЖЕТ смотреть ВВЕРХ до -3.5° (не очень высоко)
• Камера МОЖЕТ смотреть ВНИЗ до -6.5° (не очень низко)
• Только 3° свободы - камера "зажата" около центра
```

---

### Пример 3: FOV = 70° (широкий - почти заблокирован)

```
Шаг 1: Половина FOV
half_fov_v = 70° / 2 = 35°

Шаг 2: Рассчитываем границы
pitch_min = -32° + 35° + 0.5° = +3.5°
pitch_max = +22° - 35° - 0.5° = -13.5°

ПРОБЛЕМА! pitch_min > pitch_max ❌

Шаг 3: "Схлопывание" границ
center = (-32° + 22°) / 2 = -5°
pitch_min = -5° - 1° = -6°
pitch_max = -5° + 1° = -4°

Результат:
┌────────────────────────────────────────────────┐
│ Диапазон движения: от -6° до -4° = 2°         │
│ Свобода движения: ❌ ПОЧТИ ЗАБЛОКИРОВАНО       │
└────────────────────────────────────────────────┘

Что это означает:
• Камера почти ЗАФИКСИРОВАНА на -5° (центр панорамы)
• Может чуть-чуть двигаться вверх/вниз (±1°)
• Практически нет свободы вертикального движения
```

**Почему нельзя больше двигаться?**

```
Визуализация при FOV=70°:

Если камера на pitch = -5° (центр):
  Камера видит от:
    Верх: -5° + 35° = +30°  ❌ (выходит за +22° на 8°!)
    Низ:  -5° - 35° = -40°  ❌ (выходит за -32° на 8°!)

Панорама:         [-32°===============+22°]  (54° высота)
Камера видит:  [-40°==================+30°]  (70° FOV)
                     ↑ 8° чёрная ↑         ↑ 8° чёрная ↑

Камера ВЫХОДИТ за границы на 8° сверху и снизу!

Решение: Зафиксировать около центра ±1°
```

---

## 🎯 Визуализация ограничений

### Таблица границ для разных FOV:

| FOV | half_fov | pitch_min | pitch_max | Диапазон | Свобода движения |
|-----|----------|-----------|-----------|----------|-----------------|
| 40° | 20°      | -11.5°    | +1.5°     | 13°      | ✅ Полная        |
| 45° | 22.5°    | -9.0°     | -1.0°     | 8°       | ✅ Хорошая       |
| 50° | 25°      | -6.5°     | -3.5°     | 3°       | ⚠️ Ограничена    |
| 55° | 27.5°    | -4.0°     | -6.0°     | 2° (fallback) | ⚠️ Минимальная |
| 60° | 30°      | -1.5°     | -8.5°     | 2° (fallback) | ❌ Почти нет   |
| 65° | 32.5°    | +1.0°     | -11.0°    | 2° (fallback) | ❌ Почти нет   |
| 70° | 35°      | +3.5°     | -13.5°    | 2° (fallback) | ❌ Почти нет   |

---

## 🔧 Почему именно так

### Геометрия проблемы:

```
Панорама имеет фиксированный размер:
┌─────────────────────────────────────┐
│  +22° ← Верх поля                   │
│                                     │
│  -5°  ← Центр панорамы              │
│                                     │
│  -32° ← Низ поля                    │
└─────────────────────────────────────┘
Высота: 54°

Камера с большим FOV "видит" больше:
┌─────────────────────────────────────┐
│  +30° ← Верх кадра (FOV=70°)        │ ← ЭТО УЖЕ ВНЕ ПАНОРАМЫ!
├─────────────────────────────────────┤
│  +22° ← Начало панорамы             │
│                                     │
│  -5°  ← Центр камеры                │
│                                     │
│  -32° ← Конец панорамы              │
├─────────────────────────────────────┤
│  -40° ← Низ кадра (FOV=70°)         │ ← ЭТО УЖЕ ВНЕ ПАНОРАМЫ!
└─────────────────────────────────────┘
FOV камеры: 70°

Если камера двигается ВВЕРХ или ВНИЗ → ещё больше выйдет за границы!
Решение: Ограничить движение камеры.
```

---

## ⚙️ Стартовый FOV = 70°

### Настройки при запуске:

```cpp
// В nvdsvirtualcam_config.h:
DEFAULT_FOV = 70.0f

// В test_virtual_camera_keyboard.py:
fov=70
```

**При запуске теста:**
```
Стартовый FOV: 70°
Стартовый pitch: 15° → обрезается до -6° до -4°
Вертикальное движение: ±1° (минимальное)
Горизонтальное движение: ±27° (нормальное)
```

**Что видит пользователь:**
- Широкий угол обзора поля ✅
- Камера почти не двигается вверх/вниз (W/S почти не работают) ⚠️
- Камера нормально двигается влево/вправо (A/D работают) ✅

---

## 🎮 Как управлять ограничениями

### Чтобы получить больше свободы движения:

**Уменьшите FOV (клавиша Q):**
```
FOV 70° → 60° → 50° → 40°

По мере уменьшения FOV:
• Диапазон pitch растёт (2° → 3° → 13°)
• Камера получает больше свободы движения
• W/S клавиши начинают работать лучше
```

**Логи покажут изменение границ:**
```
INFO: FOV=70.0° → Границы: yaw[-27.3, 27.3], pitch[-6.0, -4.0]
INFO: FOV=50.0° → Boundaries: yaw[-44.5, 44.5], pitch[-6.5, -3.5]
INFO: FOV=40.0° → Boundaries: yaw[-53.5, 53.5], pitch[-11.5, 1.5]
```

---

## 🔍 Отладка ограничений

### Включить детальные логи:
```bash
GST_DEBUG=nvdsvirtualcam:3 python3 test_virtual_camera_keyboard.py ...
```

### Что смотреть в логах:

**При изменении FOV на 5° или больше:**
```
INFO: FOV=70.0° → Границы: yaw[-27.3, 27.3], pitch[-6.0, -4.0]
```

**Расшифровка:**
- `yaw[-27.3, 27.3]` - камера может поворачиваться влево/вправо на ±27.3°
- `pitch[-6.0, -4.0]` - камера может наклоняться только в диапазоне -6° до -4° (2° свободы)

---

## 💡 Рекомендации

### Для слежения за мячом:
```
Используйте FOV = 40-50°
✅ Полная свобода вертикального движения
✅ Камера следит за мячом везде
✅ W/S клавиши работают отлично
```

### Для широкого обзора:
```
Используйте FOV = 60-70°
✅ Широкий угол обзора поля
⚠️ Ограниченное вертикальное движение
⚠️ W/S клавиши почти не работают
✅ A/D клавиши работают нормально
```

---

## 📋 Итого

### Ограничение ВВЕРХ (pitch_max):
```
pitch_max = +22° - (FOV/2) - 0.5°

FOV=40°: pitch_max = +1.5°   ← Можно смотреть довольно высоко
FOV=50°: pitch_max = -3.5°   ← Уже ниже центра
FOV=70°: pitch_max = -4.0°   ← Почти зафиксировано
```

### Ограничение ВНИЗ (pitch_min):
```
pitch_min = -32° + (FOV/2) + 0.5°

FOV=40°: pitch_min = -11.5°  ← Можно смотреть довольно низко
FOV=50°: pitch_min = -6.5°   ← Уже выше центра
FOV=70°: pitch_min = -6.0°   ← Почти зафиксировано
```

### Схлопывание границ:
```
Когда pitch_min > pitch_max:
  → Оставляем диапазон center ±1°
  → Камера почти заблокирована
  → Это НОРМАЛЬНО при большом FOV!
```

---

✅ **Система работает корректно!**
Ограничения нужны чтобы камера не показывала чёрные области за пределами панорамы.
