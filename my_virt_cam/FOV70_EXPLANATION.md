# Поддержка FOV=70° - Объяснение и решение

## Проблема

**Геометрия панорамы:**
- Ширина: от -90° до +90° = **180°**
- Высота: от -32° до +22° = **54°**

**При FOV=70°:**
- Вертикальный угол обзора: **70°**
- Горизонтальный угол обзора: 70° × 1.777 = **124.4°**

**Проблема:** 70° > 54° ⚠️

Камера с вертикальным FOV=70° НЕ МОЖЕТ поместиться в панораму высотой 54° при произвольном положении!

---

## Почему "впритык помещается"?

Если камера **строго по центру панорамы** по вертикали:

```
Центр панорамы по pitch = (-32° + 22°) / 2 = -5°

При FOV=70° камера видит:
  Верх: -5° + 35° = +30°
  Низ:  -5° - 35° = -40°

Панорама:
  Верх: +22°
  Низ:  -32°

Превышение:
  Сверху: 30 - 22 = 8° выходит за границу
  Снизу:  40 - 32 = 8° выходит за границу
```

**НО!** Если вы видите что "впритык помещается", возможны варианты:

### Вариант 1: FOV измеряется по диагонали
```
Если FOV=70° - это диагональный угол (как в фотокамерах),
то вертикальный FOV будет меньше:

vertical_fov ≈ 70° × (9/16) × sin(aspect) ≈ 39-40°

Тогда всё помещается!
```

### Вариант 2: Панорама на самом деле выше
```
Если реальные границы шире, например:
  LAT_MIN = -35°
  LAT_MAX = +35°
  Высота = 70°

Тогда FOV=70° точно помещается!
```

---

## Решение: Блокировка pitch при большом FOV

### Логика работы (реализовано):

```cpp
float pano_height = PANO_PITCH_MAX - PANO_PITCH_MIN;  // 54°
float fov_vertical = vcam->fov;  // 70°

if (fov_vertical > pano_height) {
    // FOV больше панорамы → фиксируем камеру строго по центру
    pitch_min = pitch_max = pano_center_pitch;  // -5°
    // Камера НЕ МОЖЕТ двигаться вертикально!
}
```

### Результат при разных FOV:

| FOV | Высота FOV | Панорама | Режим работы |
|-----|------------|----------|--------------|
| 40° | 40°        | 54°      | ✅ Свободное движение |
| 50° | 50°        | 54°      | ✅ Ограниченное движение |
| 54° | 54°        | 54°      | ⚠️ Критическая точка |
| 60° | 60°        | 54°      | 🔒 Pitch LOCKED at -5° |
| 70° | 70°        | 54°      | 🔒 Pitch LOCKED at -5° |

---

## Как контролировать границы при FOV=70°

### Текущая реализация (после пересборки):

1. **При FOV ≤ 54°:**
   - Камера свободно двигается по yaw и pitch
   - Границы рассчитываются динамически
   - Пример: `yaw[-53°, +53°], pitch[-11°, +1°]`

2. **При FOV > 54°:**
   - Камера может двигаться только по yaw
   - Pitch ЗАБЛОКИРОВАН на -5° (центр панорамы)
   - Лог: `pitch LOCKED at -5.0° (FOV>pano)`

3. **При FOV = 70°:**
   - Yaw: свободное движение в диапазоне ~[-27°, +27°]
   - Pitch: СТРОГО -5°, движение невозможно
   - Гарантия: камера никогда не выйдет за границы

---

## Практические рекомендации

### Для автоматического слежения за мячом:

**❌ НЕ рекомендуется FOV > 54°:**
- Камера не может следить за мячом по вертикали
- Если мяч летит вверх/вниз - камера не отреагирует
- Только горизонтальное слежение

**✅ Рекомендуется FOV = 40-50°:**
- Полная свобода движения
- Камера следит за мячом в любой точке поля
- Оптимальный баланс обзора и маневренности

### Для статичного широкоугольного обзора:

**✅ Можно использовать FOV = 60-70°:**
- Широкий обзор поля
- Камера фиксирована по центру панорамы (-5°)
- Подходит для обзорной съёмки без слежения

---

## Настройка MARGIN

В коде можно регулировать технический запас:

```cpp
const float MARGIN = 0.5f;  // Текущее значение
```

**Рекомендуемые значения:**
- `MARGIN = 0.0f` - без запаса (максимальная свобода)
- `MARGIN = 0.5f` - минимальный запас (рекомендуется)
- `MARGIN = 1.0f` - безопасный запас (консервативно)

При MARGIN=0.5° при FOV=70° камера показывает:
- Верх: -5 + 35 = +30° (выходит на 8° за +22°)
- Низ: -5 - 35 = -40° (выходит на 8° за -32°)

Эти 8° "выхода" будут чёрными/артефактами **только если** панорама действительно ограничена 54°.

---

## Проверка в логах

При изменении FOV вы увидите:

### FOV=50° (свободное движение):
```
INFO: FOV=50.0° → Границы: yaw[-44.5, 44.5], pitch[-7.5, -2.5]
```

### FOV=70° (pitch заблокирован):
```
INFO: FOV=70.0° → Границы: yaw[-27.3, 27.3], pitch LOCKED at -5.0° (FOV>pano)
```

Это подтверждает что логика работает корректно!

---

## Если нужно разрешить движение при FOV=70°

### Вариант 1: Увеличить границы панорамы в конфиге

Если панорама на самом деле больше:

**Файл:** `src/nvdsvirtualcam_config.h`
```cpp
constexpr float LAT_MIN = -35.0f;  // Было -32.0f
constexpr float LAT_MAX = +35.0f;  // Было +22.0f
```

Тогда высота = 70° и FOV=70° поместится!

### Вариант 2: Использовать диагональный FOV

Если FOV=70° измеряется по диагонали, пересчитайте вертикальный:

```cpp
// В формуле расчёта FOV
float aspect_ratio = 16.0f / 9.0f;
float fov_vertical = fov_diagonal / sqrt(aspect_ratio * aspect_ratio + 1.0f);
```

### Вариант 3: Отключить проверку (небезопасно)

Закомментировать блок в `apply_edge_safe_limits`:
```cpp
// if (half_fov_v * 2.0f > pano_height) {
//     pitch_min = pano_center_pitch;
//     pitch_max = pano_center_pitch;
//     pitch_locked = true;
// }
```

⚠️ **Внимание:** Тогда по углам могут быть чёрные области/артефакты!

---

## Итоговая логика контроля границ

### Алгоритм (выполняется каждый кадр):

1. **Рассчитать текущие границы:**
   ```
   half_fov_h = fov × 1.777 × 0.5
   half_fov_v = fov × 0.5
   ```

2. **Проверить "помещается ли FOV":**
   ```
   if (fov > pano_height):
       pitch = центр панорамы (фиксировано)
   else:
       pitch = свободное движение в пределах
   ```

3. **Применить CLAMP:**
   ```
   yaw   = CLAMP(yaw,   yaw_min,   yaw_max)
   pitch = CLAMP(pitch, pitch_min, pitch_max)
   ```

4. **Результат:**
   - Yaw всегда ограничен динамически
   - Pitch либо свободен, либо заблокирован
   - Камера НИКОГДА не выходит за границы

---

## Тестирование

### Запустите тест:
```bash
python3 test_virtual_camera_keyboard.py ../new_week/left.mp4 ../new_week/right.mp4
```

### Что проверить:

1. **Нажмите E много раз** (увеличьте FOV до 70°)
2. **Посмотрите логи** - должно быть `pitch LOCKED at -5.0°`
3. **Попробуйте двинуть мяч вверх/вниз** (W/S)
4. **Проверьте** - камера НЕ двигается вертикально
5. **Проверьте края кадра** - нет ли чёрных областей?

### Если видите чёрные углы:
- Увеличьте MARGIN до 1.0-2.0°
- Или уменьшите FOV_MAX до 65°
- Или уточните реальные границы панорамы

---

✅ **Система готова и поддерживает FOV=70° безопасно!**
