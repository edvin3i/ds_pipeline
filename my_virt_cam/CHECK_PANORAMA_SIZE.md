# Проверка реального размера панорамы

## ❓ Главный вопрос

**Если FOV=70° "помещается" в панораму без чёрных полос - значит панорама БОЛЬШЕ 54°!**

---

## 🧪 Как проверить реальный размер панорамы

### Тест 1: Визуальная проверка при FOV=70°

Запустите тест с FOV=70°:
```bash
python3 test_virtual_camera_keyboard.py ../new_week/left.mp4 ../new_week/right.mp4
```

**Проверьте:**
1. Есть ли **чёрные полосы** сверху и снизу кадра?
2. Весь ли кадр **заполнен** изображением?

**Если видите чёрные полосы (~123px сверху и снизу):**
```
✅ Панорама = 54° (текущие настройки правильные)
   LAT_MIN = -32°
   LAT_MAX = +22°
```

**Если чёрных полос НЕТ, весь кадр заполнен:**
```
❌ Панорама БОЛЬШЕ 54°!
   Нужно измерить реальные границы
```

---

### Тест 2: Проверка границ вручную

#### Шаг 1: Проверка ВЕРХНЕЙ границы

```bash
# Запустите тест
python3 test_virtual_camera_keyboard.py ...

# В процессе работы:
# 1. Нажмите Q много раз (уменьшаем FOV до 40°)
# 2. Нажмите W много раз (двигаем камеру ВВЕРХ)
# 3. Камера должна упереться в pitch_max

# Логи покажут:
# "FOV=40.0° → Boundaries: yaw[...], pitch[-11.5, +1.5]"
# pitch_max = +1.5°

# Смотрите на экран:
# - Если видите ВЕРХНИЙ край панорамы (небо, верх стадиона)
#   → LAT_MAX = +22° правильно
# - Если кадр полностью заполнен, можно подняться ещё
#   → LAT_MAX > +22°
```

#### Шаг 2: Проверка НИЖНЕЙ границы

```bash
# 1. Нажмите S много раз (двигаем камеру ВНИЗ)
# 2. Камера должна упереться в pitch_min

# Логи покажут:
# "FOV=40.0° → Boundaries: yaw[...], pitch[-11.5, +1.5]"
# pitch_min = -11.5°

# Смотрите на экран:
# - Если видите НИЖНИЙ край панорамы (дальнее поле)
#   → LAT_MIN = -32° правильно
# - Если кадр полностью заполнен, можно опуститься ещё
#   → LAT_MIN < -32°
```

---

### Тест 3: Измерение по исходному видео

Откройте исходную панораму (`left.mp4` или результат stitching):

```python
import cv2

# Загрузите кадр панорамы
cap = cv2.VideoCapture('../new_week/left.mp4')
ret, frame = cap.read()
height, width = frame.shape[:2]

print(f"Размер панорамы: {width}x{height}")

# Проверьте края:
# 1. Посмотрите на y=0 (верхний край)
#    - Есть ли там полезный контент?
#    - Или чёрная полоса?

# 2. Посмотрите на y=height-1 (нижний край)
#    - Есть ли там полезный контент?
#    - Или чёрная полоса?

# 3. Измерьте где РЕАЛЬНО начинается/кончается контент
```

---

## 📐 Как пересчитать границы если панорама больше

### Если панорама на самом деле 70° высотой:

```
Центр панорамы должен быть на 0° (горизонт)
Симметрично: от -35° до +35°
```

**Новые настройки:**
```cpp
// В nvdsvirtualcam_config.h:
constexpr float LAT_MIN = -35.0f;  // Было -32.0f
constexpr float LAT_MAX = +35.0f;  // Было +22.0f
```

**Пересборка:**
```bash
cd src && make clean && make
```

---

### Если панорама асимметрична но больше 54°:

Нужно **измерить** реальные углы.

**Метод измерения:**

1. Найдите **контрольную точку** на панораме с известной высотой
   (например, верхний край ворот, флагшток, etc.)

2. Измерьте её **Y-координату** на панораме

3. Вычислите **реальный угол**:
   ```python
   # Текущая формула предполагает LAT_MIN=-32, LAT_MAX=+22
   # Если объект на высоте H метров на расстоянии D метров:
   real_angle = atan(H / D) * 180 / pi

   # Сравните с расчётным по формуле:
   calculated_angle = 22 - (y / 1899) * 54

   # Разница покажет ошибку
   ```

---

## 🎯 Практический тест ПРЯМО СЕЙЧАС

### Быстрый способ проверить:

1. **Запустите с FOV=70°**
2. **Нажмите ESC чтобы сделать скриншот** (или сфотографируйте экран)
3. **Посмотрите на края кадра:**

```
┌────────────────────────────────┐
│ ← Если ЗДЕСЬ чёрная полоса     │ Верх кадра
├────────────────────────────────┤
│                                │
│        ПАНОРАМА                │
│      (изображение)             │
│                                │
├────────────────────────────────┤
│ ← Если ЗДЕСЬ чёрная полоса     │ Низ кадра
└────────────────────────────────┘
```

**Результат:**
- ✅ Есть чёрные полосы → LAT_MIN/MAX правильные
- ❌ Нет чёрных полос → LAT_MIN/MAX нужно увеличить

---

## 💡 Почему это важно?

### Если границы неправильные:

**Слишком узкие (54° вместо реальных 70°):**
- ❌ Система думает что панорама меньше чем есть
- ❌ Ограничения слишком строгие
- ❌ Камера не использует весь доступный диапазон

**Правильные границы:**
- ✅ Камера использует ВСЮ доступную панораму
- ✅ Ограничения точные
- ✅ Нет лишних чёрных полос при большом FOV

---

## 🔧 Что делать дальше?

### Сценарий 1: Чёрных полос НЕТ

1. Измерьте реальные границы панорамы
2. Обновите LAT_MIN и LAT_MAX в конфиге
3. Пересоберите плагин
4. Перезапустите тест
5. Проверьте что теперь ограничения работают правильно

### Сценарий 2: Чёрные полосы ЕСТЬ

1. Всё настроено правильно!
2. Панорама действительно 54°
3. При FOV=70° будут чёрные полосы - это нормально
4. Используйте FOV ≤ 54° чтобы не было чёрного

---

## 📊 Ожидаемые результаты

### Если панорама = 54° (текущие настройки):

| FOV | Чёрные полосы? | Примечание |
|-----|---------------|------------|
| 40° | Нет | Полностью заполнено ✅ |
| 50° | Нет | Полностью заполнено ✅ |
| 54° | Нет (впритык) | Ровно заполняет ✅ |
| 60° | Да, ~5.5% сверху+снизу | Часть кадра чёрная |
| 70° | Да, ~11.4% сверху+снизу | Видны чёрные полосы |

### Если панорама = 70° (нужно обновить настройки):

| FOV | Чёрные полосы? | Примечание |
|-----|---------------|------------|
| 40° | Нет | Полностью заполнено ✅ |
| 60° | Нет | Полностью заполнено ✅ |
| 70° | Нет (впритык) | Ровно заполняет ✅ |
| 80° | Да | Часть кадра чёрная |

---

## ✅ Итоговый чеклист

- [ ] Запустил тест с FOV=70°
- [ ] Проверил наличие чёрных полос
- [ ] Если полосы есть → всё правильно
- [ ] Если полос нет → измерил реальные границы
- [ ] Обновил LAT_MIN/MAX если нужно
- [ ] Пересобрал плагин
- [ ] Перезапустил и проверил снова

---

**Проверь визуально и сообщи результат!** 🔍
